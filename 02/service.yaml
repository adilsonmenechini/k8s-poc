apiVersion: v1
kind: Service
metadata:
  labels:
    app: poc-nginx
  name: poc-nginx
spec:
  ports:
  - name: 8080-80
    port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: poc-nginx
  type: ClusterIP
status:
  loadBalancer: {}
